# --- web ---
# fastapi 0.115.6+ requires starlette >= 0.40.0 which fixes CVE-2024-47874
# (DoS via unbounded multipart forms). Anything older is reported as
# vulnerable by Docker Scout.
fastapi==0.115.6
# starlette is pulled in transitively by fastapi, but we pin it explicitly
# so the CVE scanner can verify the resolved version.
starlette>=0.40.0,<0.42.0
uvicorn[standard]==0.32.1
websockets==13.1
# python-multipart 0.0.22 fixes BOTH CVE-2024-53981 AND CVE-2026-24486
# (the boundary parsing & header limit issues). 0.0.26 is the latest and
# includes all post-0.0.22 patches.  We don't actually call it directly
# (no Form/File/UploadFile in our API), it's only here so Scout can
# verify the resolved version.
python-multipart==0.0.26

# --- data / db ---
pydantic==2.9.2
pydantic-settings==2.5.2
sqlmodel==0.0.22
aiosqlite==0.20.0
greenlet==3.1.1            # required by SQLAlchemy async

# --- http / scraping ---
curl_cffi==0.7.3            # Cloudflare-friendly TLS impersonation
beautifulsoup4==4.12.3
lxml==5.3.0
tenacity==9.0.0

# --- utils ---
python-slugify==8.0.4
aiofiles==24.1.0
structlog==24.4.0
apscheduler==3.10.4

# --- subtitles (optional at runtime — import guarded) ---
subliminal==2.2.1

# --- discord bot (embedded, managed by services/discord_bot/runner.py) ---
discord.py==2.4.0
